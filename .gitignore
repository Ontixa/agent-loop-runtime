# Dependencies
node_modules/

# Build output
dist/

# Logs
logs/*.log

# Runtime state — mission records, worktrees, logs (never commit)
.agentloop/

# Local configuration (agent paths/secrets — commit .example files instead)
qwen-loop.config.json
agentloop.config.json
# NOTE: agentloop.policy.json is intentionally NOT ignored — it's the
# reviewable safety contract for the repo.

# Test artifacts
src/test.ts
hello.txt

# Environment variables
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# TypeScript
*.tsbuildinfo
